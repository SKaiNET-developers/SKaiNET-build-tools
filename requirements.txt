# Docker image Python dependencies for IREE compilation service
# These are the minimal runtime dependencies needed inside Docker containers

# Core validation and configuration
jsonschema>=4.0.0
pydantic>=2.0.0

# CLI and utilities
click>=8.0.0
pyyaml>=6.0.0

# Rich output formatting for better user experience
rich>=13.0.0

# Docker SDK for container management (if needed inside container)
docker>=6.0.0

# Additional utilities for file handling and path operations
pathlib2>=2.3.0; python_version < "3.4"